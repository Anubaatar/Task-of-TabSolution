/*import { gql } from "apollo-server-micro";

export const typeDefs = gql`#graphql
    
    type Query{ 
        status: [Staff]
    }

    type Mutation {
        createUser(name: String): Staff
    }

    type Staff{
        id: String
        name: String
        email: String
        phone: String
        address: String
        birthday: String
        sex: String
    }

`*/